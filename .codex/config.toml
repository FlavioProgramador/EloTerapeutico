# Configuração de subagentes do projeto Elo Terapêutico.
# Os agentes herdam o modelo da sessão principal para evitar incompatibilidade
# com modelos que possam não estar disponíveis na conta.

[agents]
max_threads = 8
max_depth = 1
job_max_runtime_seconds = 1800
