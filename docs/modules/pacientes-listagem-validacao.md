# Validação da listagem de pacientes

A implementação foi revisada quanto à sintaxe dos arquivos TypeScript/TSX e Python antes da abertura do pull request.

Os comandos completos do projeto permanecem documentados em `docs/modules/pacientes-listagem.md` e devem ser executados pelo CI ou no ambiente Docker do repositório.
