name: Test Coverage

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  backend-coverage:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: echo coverage
