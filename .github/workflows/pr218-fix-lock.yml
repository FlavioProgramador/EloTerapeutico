name: PR218 Lockfile Repair

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/pr218-fix-lock.yml"

permissions:
  contents: read

jobs:
  repair-lockfile:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout pull request
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Apply secure transitive dependency overrides
        working-directory: frontend
        shell: bash
        run: |
          npm pkg set 'overrides.sharp=^0.35.0'
          npm pkg set 'overrides.postcss=^8.5.10'
          npm update brace-expansion js-yaml --package-lock-only --ignore-scripts
          npm install --package-lock-only --ignore-scripts

      - name: Validate clean installation
        working-directory: frontend
        run: npm ci --ignore-scripts

      - name: Run frontend tests
        working-directory: frontend
        run: npm test

      - name: Build Next.js application
        working-directory: frontend
        run: npm run build

      - name: Generate complete npm audit report
        working-directory: frontend
        shell: bash
        run: npm audit --json > npm-audit.json || true

      - name: Upload repaired manifests and audit report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pr218-lockfile-diagnostics
          path: |
            frontend/package.json
            frontend/package-lock.json
            frontend/npm-audit.json
          retention-days: 1

      - name: Validate high severity audit gate
        working-directory: frontend
        run: npm audit --audit-level=high
